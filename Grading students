from math import ceil, floor
x=int(input())
for i in range(x):
    y=int(input())
    g=ceil(y/5)
    h=g*5
    if y>37:
        if (h-y)<3:
            print(h)
        else:
            print(y)
    else:
        print(y)
